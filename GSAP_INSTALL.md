# GSAP Installation Required

To use the new graph positioning modes, you need to install GSAP:

```bash
npm install gsap
```

Or if using yarn:

```bash
yarn add gsap
```

This enables smooth animations when switching between positioning modes and during auto-layout.