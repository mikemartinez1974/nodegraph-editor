{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile-vscode-extension",
            "type": "shell",
            "command": "npm",
            "args": [
                "run",
                "compile"
            ],
            "options": {
                "cwd": "${workspaceFolder}\\idePlugin\\VSCode"
            },
            "problemMatcher": "$tsc",
            "group": "build"
        }
    ]
}
