import React from 'react';

const EdgeLayer = ({ edges, onEdgeEvent }) => {
    return (
        <div style={{ pointerEvents: 'none' }}>
            {/* ...existing code... */}
        </div>
    );
};

export default EdgeLayer;